# Kill

Kills a target **Player** (skips the Mandown state).

### Inputs

1. Player

```blockly
<block type="Kill">
    <value name="VALUE-0">
        <block type="EventPlayer"></block>
    </value>
</block>
```
